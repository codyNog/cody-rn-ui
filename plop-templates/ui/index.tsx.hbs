'use client';
import { forwardRef } from "react";
import { Text, type TamaguiElement } from "tamagui";

type Props = {
  // コンポーネントのプロパティをここに定義
}

export const {{pascalCase name}} = forwardRef<TamaguiElement, Props>((props, ref) => {
  return <Text ref={ref}>{{pascalCase name}}</Text>
});
