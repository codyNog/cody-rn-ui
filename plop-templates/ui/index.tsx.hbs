'use client';
import { forwardRef } from "react";
import { Text, type TamaguiElement } from "tamagui";

// biome-ignore lint:
type Props = {
  // コンポーネントのプロパティをここに定義
}

export const {{pascalCase name}} = forwardRef<TamaguiElement, Props>((_props, ref) => {
  return <Text ref={ref}>{{pascalCase name}}</Text>
});
