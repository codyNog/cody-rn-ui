'use client';
import type { Ref } from "react";
import { Text, type TamaguiElement } from "tamagui";


type Props = {
  ref?: Ref<TamaguiElement>
}

export const {{pascalCase name}} = (({ref}: Props) => {
  return <Text ref={ref}>{{pascalCase name}}</Text>
});
